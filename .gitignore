.sass-cache
_site
.jekyll-cache

# macOS
.DS_Store

# Python (dashboard)
__pycache__/
*.pyc
.venv/

# Ruby gems for local Jekyll preview (start-site.sh)
vendor/
.bundle/

# Slides this connection cannot upload. Pushes corrupt partway through above
# roughly 10MB -- at a random offset each attempt, on port 22 and 443 alike,
# while downloads verify clean -- so these four never make it through. They
# live on disk; host them elsewhere or retry from another network.
static_files/uploads/Chapter_6_S26_P1.pdf
static_files/uploads/Chapter_6_P2.pdf
static_files/uploads/Chapter_7_s26.pdf
static_files/uploads/final-exam_overview_26.pdf
